import logo from "./logo.svg";
import "./React.css";

export const React = () => (
  <div className="App">
    <header className="App-header">
      <img src={logo} className="App-logo" alt="logo" />
      <p>Hello, from client-side routing!</p>
    </header>
  </div>
);
